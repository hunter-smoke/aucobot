# Channels

OAuth and API integrations for third-party social platforms. Each channel registers its own MCP tools (e.g. list pages, publish).

| Plugin id | Folder | Status |
|-----------|--------|--------|
| `facebook` | `facebook/` | stub |
| `tiktok` | `tiktok/` | planned |

Publishing schedules and job execution live in `../workflow/publishing/`, not here.
