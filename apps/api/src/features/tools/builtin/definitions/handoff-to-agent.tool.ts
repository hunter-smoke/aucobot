import type { McpToolDefinition } from "../../../../core/plugins/feature-plugin.interface";

export const handoffToAgentTool: McpToolDefinition = {
  name: "handoff_to_agent",
  description:
    "Transfer the current task to another agent in the same marketing department",
};
