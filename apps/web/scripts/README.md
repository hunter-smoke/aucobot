# `scripts/`

Dev / CI tooling — **không** import vào `app/`, `components/`, `lib/`, `hooks/`, `utils/`.

## Planned

- E2E Playwright scripts (khi có)
- Codegen helpers

Chạy qua `package.json` scripts — không bundle vào Next.js app.
