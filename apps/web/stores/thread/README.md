# `stores/thread/`

## Planned: `thread.store.ts`

| State | Mô tả |
|-------|--------|
| `threads` | Danh sách department (thread) từ API |
| `selectedDepartmentId` | Thread đang mở |
| `unreadByDepartmentId` | Badge (optional, từ API/WS) |

MVP 1 department/user: list có thể 1 phần tử — vẫn giữ model list.
