# `app/_components/`

Shared components dùng trong `app/` (không thuộc route group cụ thể):

| Component | Ghi chú |
|-----------|---------|
| `StatusBadge/` | Health indicator — có thể chuyển `components/ui/` sau |

Auth UI: `app/(auth)/` + `components/auth/`.
