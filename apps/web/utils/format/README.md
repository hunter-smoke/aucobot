# `utils/format/`

## Planned

| File | Mô tả |
|------|--------|
| `format-time.ts` | Relative time (“5 phút trước”) — Luxon/date-fns-tz |
| `format-schedule.ts` | Hiển thị giờ đăng `Asia/Ho_Chi_Minh` |

Chỉ presentation — timezone canonical do API quyết định.
