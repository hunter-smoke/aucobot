# `utils/`

Helper **thuần** — format hiển thị, không I/O, không React.

## Domains

| Folder | Ví dụ |
|--------|--------|
| [`chat/`](./chat/README.md) | `merge-stream-chunks.ts` |
| [`format/`](./format/README.md) | `format-time.ts`, timezone display |

## Cấm

- `fetch`, `axios`, WebSocket
- Import `hooks/`, `components/`, `lib/api/`

Được: `import type`, `@aucobot/shared`.
