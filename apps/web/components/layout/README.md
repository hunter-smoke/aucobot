# `components/layout/`

Bố cục app Telegram-style.

## Planned components

| Component | Mô tả |
|-----------|--------|
| `AppShell/` | Root layout 2 cột |
| `SplitPane/` | List \| content (desktop) |
| `ThreadList/` | Sidebar danh sách phòng |
| `Composer/` | Ô nhập tin + nút gửi |
| `Container/` | Max-width wrapper (nếu cần) |

Không fetch. Nhận `children` + props layout.
